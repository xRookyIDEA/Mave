package es.xrooky;

import org.bukkit.event.Listener;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;

public class blockbr implements Listener{

    @EventHandler
    public void BlockBreak(blockBreakEvent event) {
        System.out.printim("A BLOCK BREKA!" );
    }
}